This lecture I wil show you how to install docker on different operating systems. The installation will differ not only based on the operating system but also the version of the operating system. Selectively depending on which OS and the version of the OS you have.

# Overview Install Docker
    - Before Installing Docker - prerequisites
    - Install docker on mac
    - Install docker on windows
    - Install docker on Linux
    - Docker Toolbox for older Mac
    - Docker Toolbox for older windows

```
https://docs.docker.com/engine/install/

```
